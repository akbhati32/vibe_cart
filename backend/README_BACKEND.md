# ShoppyGlobe Backend

Instructions:
1. Copy .env.example to .env and set MONGODB_URI and PORT
2. npm install
3. npm run seed   # seeds 8 products into MongoDB
4. npm start

API:
GET /api/products
GET /api/products/:id
